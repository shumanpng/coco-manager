---
id: B006
title: Clean up unused instructions/files
status: todo
horizon: next
priority: P2
impact: low
effort: low
risk: low
owner:
kind: engineering
tags: [maintenance, cleanup, open-source]
blocked_by: []
blocking: []
related: []
created_on: 2026-02-22
updated_on: 2026-04-24
due_by:
---

<!-- Sanitized example from the context-manager project's own development. Some details have been redacted. -->

### Problem / Goal

The repo may contain leftover instruction and template files that are no longer referenced by any skill (e.g., old single-path `meta.md` format references, superseded instruction files). These add noise and can confuse future contributors or skills.

### Proposed approach

Audit all files under `<notes-dir>/` and `.claude/skills/` to determine which are actively referenced by a skill or template, and either archive or remove unreferenced files.

### Dependencies / blockers

None.

### Next smallest step

List all `.md` files under `<notes-dir>/` and `.claude/skills/` and cross-reference each against the `Read` calls in all skill files to identify which files are never referenced.

### Success looks like

Every file in `<notes-dir>/` and `.claude/skills/` is either referenced by a skill, is a template, is a generated output, or is explicitly marked as archived — no orphaned files remain.

### Evidence / pointers

- Related state items:
- Worklogs: `worklogs/2026-03-02_add_build_week_skill.md` (partial cleanup: deleted `constraints.md`, archived `_weekly_planner_agent.md`; full audit still pending)
- Artifacts: none
- Code/scripts: `.claude/skills/`
